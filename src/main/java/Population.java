public class Population{
    SELECT countries
}

    .no, country.first_name, country.last_name, population
        FROM countries, population
        WHERE countries.no = countries.no AND population.to_date